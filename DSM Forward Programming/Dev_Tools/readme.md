## DSM DEBUG

Usefull tool to decode captured DSM/DSMX frames, and have an idea of what is the conversation between the TX and RX.
Currently can decode Forward Pogramming Frames, as well as some other Telemetry channels.

The data directory has the .CSV file with RAW data captures.